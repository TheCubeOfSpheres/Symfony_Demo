{% extends 'base.html.twig' %}

{% block title %}Edit Contacts{% endblock %}

{% block body %}
    <div class="container">
    	<h4 style="padding: 15px">Edit Contacts</h4>
    	<div class="col-xl-12">
        	<div class="contact-form-wrapper">
        	    <div class="row">
					{{ include('admin_contacts/_form.html.twig', {'button_label': 'Update'}) }}
				</div>
				<div class="row">
					<a href="{{ path('admin_contacts_index') }}">back to list</a>
				</div>
				<div class="row">
					{{ include('admin_contacts/_delete_form.html.twig') }}				
				</div>
			</div>
		</div>
	</div>
{% endblock %}
